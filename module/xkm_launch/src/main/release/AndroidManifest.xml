<manifest xmlns:android="http://schemas.android.com/apk/res/android"

    package="com.skyseraph.xknife.mod.launch">

    <application
        android:allowBackup="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"
        android:label="@string/app_name">

        <activity android:name=".impl.view.GuideActivity"></activity>

        <activity android:name=".impl.view.SplashActivity"></activity>
    </application>

</manifest>
